name: smolvlm-500m
model_id: HuggingFaceTB/SmolVLM-500M-Instruct
architecture: image_text_to_text
load_in_4bit: false
dtype: float16
processor_longest_edge: 1024
max_new_tokens: 64
notes: >-
  Deadline-safe local model. The official card reports about 1.23 GB GPU RAM
  for one-image inference, so use FP16 rather than runtime quantization.
